#!/bin/sh
# Pre-push hook:
#   1. Incrementa PATCH en VERSION
#   2. Ammend al último commit (incorpora el cambio en lo que se va a pushear)
#   3. Crea tag vVERSION
set -e

ROOT="$(git rev-parse --show-toplevel)"
VERSION_FILE="$ROOT/VERSION"

CURRENT="$(cat "$VERSION_FILE" 2>/dev/null | tr -d '[:space:]')"
if [ -z "$CURRENT" ]; then
    echo "pre-push: VERSION file is empty, cannot bump"
    exit 1
fi

MAJOR="${CURRENT%%.*}"
REST="${CURRENT#*.}"
MINOR="${REST%.*}"
PATCH="${REST#*.}"
NEXT_PATCH="$((PATCH + 1))"
NEXT="${MAJOR}.${MINOR}.${NEXT_PATCH}"

echo "pre-push: bumping VERSION $CURRENT → $NEXT"
echo "$NEXT" > "$VERSION_FILE"

git add "$VERSION_FILE"

# Amend al último commit para que el cambio viaje en el push
git commit --amend --no-edit --allow-empty

TAG="v${NEXT}"
if git rev-parse "$TAG" >/dev/null 2>&1; then
    echo "pre-push: tag $TAG already exists, skipping tag creation"
else
    echo "pre-push: creating tag $TAG"
    git tag "$TAG"
fi
